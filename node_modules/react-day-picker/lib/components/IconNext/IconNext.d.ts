import * as React from 'react';
/**
 * Render the "next month" button in the navigation.
 */
export declare function IconNext({ className, style }: {
    className?: string;
    style?: React.CSSProperties;
}): JSX.Element;
