import { DayPickerContextValue, Matcher } from '../../../types';
/**
 * Returns true when the day matches against the given matcher. Supports few
 * basic matchers.
 */
export declare function isMatch(day: Date, matcher: Matcher, displayMonth: Date, props: DayPickerContextValue): boolean;
