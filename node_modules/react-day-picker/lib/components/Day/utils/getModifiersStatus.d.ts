import { DayPickerContextValue, ModifiersStatus } from '../../../types';
/**
 * Return the status of the modifiers for the given day,
 */
export declare function getModifiersStatus(day: Date, displayMonth: Date, context: DayPickerContextValue): ModifiersStatus;
