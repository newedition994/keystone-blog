import { differenceInCalendarDays } from 'date-fns';
export function matchBeforeAndAfter(day, matcher) {
    if (!('before' in matcher) || !('after' in matcher))
        return false;
    return (differenceInCalendarDays(day, matcher.before) < 0 &&
        differenceInCalendarDays(day, matcher.after) > 0);
}
//# sourceMappingURL=matchBeforeAndAfter.js.map