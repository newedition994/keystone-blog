import { isSameDay } from 'date-fns';
export function matchDate(day, matcher) {
    if (!(matcher instanceof Date))
        return false;
    return isSameDay(day, matcher);
}
//# sourceMappingURL=matchDate.js.map