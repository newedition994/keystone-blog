import { addMonths, startOfMonth } from 'date-fns';
/**
 * Returns the next and the previous months that the user can navigate to.
 */
export function getNavMonths(month, options) {
    var fromDate = options.fromDate, toDate = options.toDate, pagedNavigation = options.pagedNavigation, numberOfMonths = options.numberOfMonths, disableNavigation = options.disableNavigation;
    if (disableNavigation) {
        return [undefined, undefined];
    }
    var add = pagedNavigation ? numberOfMonths : 1;
    var currentMonth = startOfMonth(month !== null && month !== void 0 ? month : options.today);
    var prevMonth;
    if (!fromDate || currentMonth > startOfMonth(fromDate)) {
        prevMonth = addMonths(currentMonth, add * -1);
    }
    var nextMonth;
    if (!toDate ||
        addMonths(currentMonth, numberOfMonths) <= startOfMonth(toDate)) {
        nextMonth = addMonths(currentMonth, add);
    }
    return [prevMonth, nextMonth];
}
//# sourceMappingURL=getNavMonths.js.map