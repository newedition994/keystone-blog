import { addMonths, differenceInMonths, startOfMonth } from 'date-fns';
export function getMonthsToRender(currentMonth, numberOfMonths, _a) {
    var toDate = _a.toDate, fromDate = _a.fromDate, reverseMonths = _a.reverseMonths;
    var start = startOfMonth(currentMonth);
    var end = startOfMonth(addMonths(start, numberOfMonths));
    var monthsDiff = differenceInMonths(end, start);
    var months = [];
    for (var i = 0; i < monthsDiff; i++) {
        var nextMonth = addMonths(start, i);
        if (toDate && nextMonth > startOfMonth(toDate))
            continue;
        if (fromDate && nextMonth < startOfMonth(fromDate))
            continue;
        months.push(nextMonth);
    }
    if (reverseMonths)
        months = months.reverse();
    return months;
}
//# sourceMappingURL=getMonthsToRender.js.map