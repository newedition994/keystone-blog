declare type GetNavMonthsOptions = {
    numberOfMonths: number;
    fromDate?: Date;
    toDate?: Date;
    pagedNavigation?: boolean;
    today?: Date;
    disableNavigation?: boolean;
};
/**
 * Returns the next and the previous months that the user can navigate to.
 */
export declare function getNavMonths(month: Date, options: GetNavMonthsOptions): [prevMonth?: Date, nextMonth?: Date];
export {};
