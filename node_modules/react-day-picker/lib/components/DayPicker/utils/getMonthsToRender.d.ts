declare type Options = {
    toDate?: Date;
    fromDate?: Date;
    reverseMonths?: boolean;
};
export declare function getMonthsToRender(currentMonth: Date, numberOfMonths: number, { toDate, fromDate, reverseMonths }: Options): Date[];
export {};
