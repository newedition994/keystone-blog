import * as React from 'react';
import { useDayPicker } from '../../hooks';
import { UIElement } from '../../types';
import { getWeekdays } from './utils/getWeekdays';
/**
 * Render the Head component - i.e. the table head with the weekday names.
 */
export function Head() {
    var _a = useDayPicker(), locale = _a.locale, classNames = _a.classNames, styles = _a.styles, labels = _a.labels, showWeekNumber = _a.showWeekNumber, formatWeekdayName = _a.formatters.formatWeekdayName;
    var weekdays = getWeekdays(locale);
    return (React.createElement("thead", { style: styles === null || styles === void 0 ? void 0 : styles[UIElement.Head], className: classNames[UIElement.Head] },
        React.createElement("tr", { style: styles === null || styles === void 0 ? void 0 : styles[UIElement.HeadRow], className: classNames[UIElement.HeadRow] },
            showWeekNumber && (React.createElement("th", { style: styles === null || styles === void 0 ? void 0 : styles[UIElement.HeadCell], className: classNames[UIElement.HeadCell] })),
            weekdays.map(function (weekday, i) { return (React.createElement("th", { key: i, scope: "col", className: classNames[UIElement.HeadCell], style: styles === null || styles === void 0 ? void 0 : styles[UIElement.HeadCell], "aria-label": labels.weekdayLabel(weekday, { locale: locale }) }, formatWeekdayName(weekday, { locale: locale }))); }))));
}
//# sourceMappingURL=Head.js.map